# Core LangChain ecosystem
langchain-classic==1.0.1
langchain-core==1.2.17
langchain-community==0.4.1
langchain-huggingface==1.2.1
langchain-text-splitters==1.1.1

# Vector store: Milvus (Milvus Lite runs in-process — no server needed)
pymilvus>=2.4.0            # includes milvus-lite
langchain-milvus>=0.1.0

# Embeddings
sentence-transformers==5.2.3
huggingface_hub==1.5.0

# Stage-1 raw index (used only by embedder.py)
faiss-cpu==1.13.2

# LLM — native HuggingFace inference (replaces Ollama)
torch>=2.1.0
transformers>=4.45.0
peft>=0.12.0
accelerate>=0.34.0

# Optional: 4-bit quantisation (GPU only, not needed on CPU)
# bitsandbytes>=0.43.1

# Data extraction (needed only for format_for_finetuning.py)
openpyxl

# Fine-tuning stack (QLoRA) — only needed if re-training
datasets>=2.20.0

# Interface
streamlit>=1.39.0
